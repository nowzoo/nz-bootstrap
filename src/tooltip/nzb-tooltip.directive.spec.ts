import { NzbTooltipDirective } from './nzb-tooltip.directive';

describe('NzbTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new NzbTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
